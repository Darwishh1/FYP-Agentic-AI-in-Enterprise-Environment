# core.autocrlf is true on this machine, so text files are checked out as CRLF.
# 'build tracker.html' is the build tracker itself and is hand-edited, so pin it to LF
# to keep its diffs readable rather than having every line flip on checkout.
*.html  text eol=lf
